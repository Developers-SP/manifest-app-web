import { ActiveOnDirective } from './active-on.directive';

describe('ActiveOnDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveOnDirective();
    expect(directive).toBeTruthy();
  });
});
