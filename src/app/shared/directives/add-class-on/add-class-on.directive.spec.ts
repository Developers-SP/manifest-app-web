import { AddClassOnDirective } from './add-class-on.directive';

describe('AddClassOnDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassOnDirective();
    expect(directive).toBeTruthy();
  });
});
